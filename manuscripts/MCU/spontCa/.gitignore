# Per-cell event files: regeneratable by mcu_spontCa (auto/), produced
# by the user via spontCa_manCur (man/), or by llmCur_assemble (llm/).
# All are local data, not code.
auto/
man/
llm/

# Legacy curation dir (pre-migration). Kept locally for safety; remove
# manually once migrated outputs in man/ are verified.
spontCa_curated/

# MATLAB editor autosaves
*.asv

# Diagnostic outputs from _diag/run_characterize.m (CSV + .md report).
# Re-run locally; not source code.
_diag/cellStats.csv
_diag/eventStats.csv
_diag/summary.md
